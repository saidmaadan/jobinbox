<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_candidate %>
        <div class='nav-search-form'>
        <%= form_tag search_jobs_path, method: :get, class: "navbar-form navbar-right" do %>
          <p>
            <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Search for your next Jobs", size: 30 %>
            <%= submit_tag 'JOB', name: nil, class: "btn btn-success" %>
          </p>
        <% end %>
        </div>
      <% elsif current_employer %>
        <div class='nav-search-form'>
          <%= form_tag search_jobs_path, method: :get, class: "navbar-form navbar-right" do %>
            <p>
              <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Search for your next Jobs", size: 30 %>
              <%= submit_tag 'JOB', name: nil, class: "btn btn-success" %>
            </p>
          <% end %>
        </div>
       <%#= link_to 'Home', current_candidate, class: 'navbar-brand' %>
      
      <%# else %>
        <%#= link_to 'Home', root_path, class: 'navbar-brand' %>
        
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <%= render "layouts/navigation_links" %>
      </ul>
    </div>
  </div>
</nav>
